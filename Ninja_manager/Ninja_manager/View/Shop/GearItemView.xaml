<Page x:Class="Ninja_manager.View.Shop.GearItemView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Ninja_manager.View.Shop"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Background="Yellow"
        DataContext="{Binding GearItem, Source={StaticResource Locator}}"
      Title="GearItemView">

    <Grid>
        <Grid x:Name="Container">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Image Grid.Column="0" HorizontalAlignment="Stretch" Height="100" VerticalAlignment="Stretch" Width="100"/>
            <Label Content="{Binding Category}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,15"/>
            <Grid x:Name="Stats" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <Label Grid.Row="0" Content="{Binding Name}" VerticalAlignment="Bottom" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,15,0,15"/>
                <Label Grid.Row="1" Content="{Binding Strength}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                <Label Grid.Row="2" Content="{Binding Agility}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,15,0,0"/>
                <Label Grid.Row="3" Content="{Binding Intelligence}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,15,0,15"/>
                <Label Grid.Row="4" Content="{Binding Price}" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,15,0,0"/>
            </Grid>
        </Grid>

    </Grid>
</Page>
